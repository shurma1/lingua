> **⚠️ Важное примечание о Requirements.txt**  
> В данном проекте **не используется Python**. Файл `Requirements.txt` создан исключительно, чтобы удовлетворить требование организаторов. Список всех используемых библиотек находится в `package.json` (frontend и server).

# Разработка

## Server

### Установка зависимостей

```bash
cd server
npm install
```

### Настройка окружения

Скопируйте файл `.env.example` в `.env` и настройте переменные окружения:

```bash
cp .env.example .env
```

Основные переменные окружения:
- `NODE_ENV` - окружение (development/production)
- `PORT` - порт сервера (по умолчанию 8000)
- `DB_HOST` - хост PostgreSQL (для локального запуска используйте `localhost`, в Docker - `postgres`)
- `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD` - настройки PostgreSQL
- `JWT_SECRET`, `JWT_REFRESH_SECRET` - секретные ключи для JWT (обязательно измените в production!)
- `BOT_TOKEN` - токен бота
- `TTS_MODEL`, `TTS_DTYPE` - настройки TTS модели

**Генерация JWT секрета:**
```bash
# Генерация JWT_SECRET
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Скопируйте сгенерированные значения в `.env JWT_SECRET=<GENERATED_SECRET>`.

### Запуск сервера

**Режим разработки:**
```bash
npm run start:dev
```

**Production сборка и запуск:**
```bash
npm run start:prod
```

**Только сборка:**
```bash
npm run build
```

## Frontend

Приложение требует авторизации через валидацию initData. Так как в Dev mode её нет, мы будем её имитировать. 
Вы можете использовать тестовый токен бота и данные с сайта MAX ([dev.max.ru/docs/webapps/validation](https://dev.max.ru/docs/webapps/validation)).

> **🔑 Пример тестового `BOT_TOKEN` (Dev):**
> ```env
> BOT_TOKEN=2Uk3Z_8zAlwhprgOcK3r1B1fDk8uhi2MDv47EvXkcu8
> ```
>
> Используйте только в локальной разработке.

**Как добавить токен:**
1. Откройте файл `server/.env` (создайте из `.env.example`, если ещё не сделали).
2. Добавьте строку `BOT_TOKEN=2Uk3Z_8zAlwhprgOcK3r1B1fDk8uhi2MDv47EvXkcu8`.

```
auth_date%3D1733485316394%26query_id%3D158b120b-7aa3-4a0f-a198-52ace06d0658%26user%3D%257B%2522language_code%2522%253A%2522ru%2522%252C%2522first_name%2522%253A%2522%25D0%2592%25D0%25B0%25D1%2581%25D1%258F%2522%252C%2522last_name%2522%253A%2522%2522%252C%2522photo_url%2522%253Anull%252C%2522username%2522%253Anull%252C%2522id%2522%253A400%257D%26hash%3Df982406d90b118d8e90e26b33c5cec0cadd3fc30354f2955c75ff8e3d14d130d
```

>  Скопируйте строку выше в `frontend/src/components/development/mockWebAppAPI.ts` на строке 80 в поле `initData`:
> ```typescript
> version: "25.9.16",
> initData: "auth_date%3D1733485316394%26query_id%3D...",
> initDataUnsafe: {}
> ```
> 
> Это необходимо для авторизации в Dev mode.


### Запуск Frontend

**Режим разработки:**
```bash
npm run dev
```

**Build:**
```bash
npm run build
```

# Сборка Docker

> **🐳 Запуск проекта в Docker-контейнерах**

### Подготовка

1. **Установите зависимости сервера:**
   ```bash
   cd server
   npm install
   ```

2. **Скопируйте `.env` файл:**
   ```bash
   # Из корня проекта
   cp server/.env .env
   ```
   
   > ⚠️ Убедитесь, что в `.env` параметр `DB_HOST=postgres` (не `localhost`), так как в Docker используется имя сервиса из `docker-compose.yml`.

3. **Запустите Docker Compose:**
   ```bash
   docker-compose up --build
   ```


# Админ-панель

> **🔐 Доступ к административной панели**

Для получения прав администратора выполните следующие шаги:

### 1. Подключитесь к PostgreSQL

**Если используете Docker:**
```bash
docker exec -it <имя_контейнера_postgres> psql -U <DB_USER> -d <DB_NAME>
```

**Локальное подключение:**
```bash
psql -h localhost -U <DB_USER> -d <DB_NAME>
```

### 2. Выполните SQL-запрос

Замените `<ВАШ_ID_ИЗ_MAX>` на ваш реальный MAX user ID:

```sql
UPDATE "user" SET role = 'admin' WHERE max_user_id = <ВАШ_ID_ИЗ_MAX>;
```

**Пример:**
```sql
UPDATE "user" SET role = 'admin' WHERE max_user_id = 400;
```

### 3. Откройте админ-панель в приложении

В приложении **5 раз подряд нажмите** на вкладку **"Курс"** в нижней панели навигации (Tab Bar) — откроется админ-панель.

> 💡 Это скрытый жест для открытия административной панели. Панель будет доступна только пользователям с ролью `admin`.
